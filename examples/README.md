# Public Examples

These scenarios contain fictional, non-destructive data and no machine-specific paths.

Copy an example into a locally created environment scenario folder, then adjust selectors for the target application. Runtime environments and scenarios under `.nexus-simulator/` are intentionally ignored by Git.
